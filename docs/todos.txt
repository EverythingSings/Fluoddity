RESTART ON OPEN
    def run(self):
        self.sim.reset()
-Timelapse
-save needs to change "current file"
-radial absolute Orientation
-camera.py -> screentotex fallback for 0 size
Rewrite slider tooltips
write tooltips for some sim controls
tune slider ranges


Orientation mode: 
    intrinsic
        velocity
        velocity field ahead or behind
        time or phase
    extrinsic
        fixed
        towards center.
        away from edges
        image based

Symmetry mode:
    local coord 1
    local coord 2
    both
